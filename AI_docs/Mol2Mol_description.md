# Deep Molecular Optimization (Mol2Mol)

This section describes the files related to the Deep Molecular Optimization (Mol2Mol) functionality within the repository.

- `utils_MMT/molformer_functions_v15_4.py`: Contains functions specifically for interacting with or utilizing the MolFormer model, likely for molecular representation or generation tasks within the MMT framework.

Note that a `models` folder exists within the repository at `/MultiModalSpectralTransformer/models`. This folder contains pre-trained model files that are too large to be included in the standard repository clone. The Mol2Mol related models are located within the `models/mol2mol` subdirectory of this `models` folder.

The `/home/user/multimodalspectraltransformer/deep-molecular-optimization` folder contains files specifically related to the deep molecular optimization or mol2mol model. its purpose is to house the code and potentially data or model files required for the molecular generation and optimization capabilities used within the larger project.
the `/home/user/multimodalspectraltransformer/deep-molecular-optimization` folder contains files specifically related to the deep molecular optimization or mol2mol model. its purpose is to house the code and potentially data or model files required for the molecular generation and optimization capabilities used within the larger project.

files within the `/home/user/multimodalspectraltransformer/deep-molecular-optimization` folder:

- add descriptions for files found in the deep-molecular-optimization folder here. (i need a list of the files in that folder to provide specific descriptions.)